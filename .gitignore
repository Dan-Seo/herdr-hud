# generated by /plugin-types inside the plugin folder; version-specific
.claude/*
!.claude/settings.json
node_modules/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Throwaway scratch files.
scratchpad/
