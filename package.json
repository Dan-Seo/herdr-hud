{
  "name": "herdr-hud",
  "version": "0.1.0",
  "private": true,
  "description": "Herdr agent status HUD above the Claude Code prompt (Claude Code mod / function hooks)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test tests/unit/client.spec.ts tests/unit/hud.spec.ts",
    "test:hooks": "claude plugin test .",
    "typecheck": "tsc -p tsconfig.json",
    "validate": "claude plugin validate ."
  }
}
